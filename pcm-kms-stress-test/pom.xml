<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.pcm.kms</groupId>
        <artifactId>pcm-kms</artifactId>
        <version>0.2.0-SNAPSHOT</version>
    </parent>
    <artifactId>pcm-kms-stress-test</artifactId>
    <name>pcm-kms-stress-test</name>
    <description>PCM-KMS 压力测试模块，支持百万级数据加解密性能验证</description>

    <dependencies>
        <!-- KMS 客户端 SDK -->
        <dependency>
            <groupId>com.pcm.kms</groupId>
            <artifactId>pcm-kms-client-starter</artifactId>
        </dependency>
        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
