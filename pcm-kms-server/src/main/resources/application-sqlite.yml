spring:
  datasource:
    url: jdbc:sqlite:${kms.sqlite.data-path:./data}/pcm-kms.db
    driver-class-name: org.sqlite.JDBC
  flyway:
    enabled: false

kms:
  datasource:
    mode: sqlite
  cache:
    type: local
