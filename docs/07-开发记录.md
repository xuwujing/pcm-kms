# PCM-KMS 开发记录

## 2026-06-10 Phase 1 Bootstrap

- 负责人：Codex
- 目标：搭建最小可运行框架，并整理文档目录
- 已完成：
  - `docs` 目录归档，旧草稿和参考资料迁移到 `docs/bak`
  - 创建 Maven 父工程 `pom.xml`
  - 创建后端基础模块：
    - `pcm-kms-common`
    - `pcm-kms-domain`
    - `pcm-kms-core`
    - `pcm-kms-infra`
    - `pcm-kms-client-starter`
    - `pcm-kms-server`
  - 创建后端启动类和最小探活接口：
    - `GET /api/system/ping`
  - 创建本地模式配置位：
    - `application.yml`
    - `application-sqlite.yml`
    - `application-mysql.yml`
  - 创建前端最小骨架：
    - `pcm-kms-admin-ui/package.json`
    - `pcm-kms-admin-ui/vite.config.js`
    - `pcm-kms-admin-ui/src/App.vue`
- 当前状态：
  - 已具备 Phase 1 的基础仓库结构
  - 后端和前端都进入"可继续补实现"的状态
- 暂未完成：
  - Maven / Vite 实际编译验证
  - MyBatis-Plus、Flyway、SQLite 驱动真实接入
  - 管理后台登录、布局、接口联调
  - KMS 核心实体和数据库初始化脚本
- 下一步建议：
  - 先补 `sql/` 初始化脚本和真实数据源配置
  - 再接 MyBatis-Plus 和 Flyway
  - 再开始做应用创建、启用、密钥生成主链路

## 2026-06-10 Phase 1.5 Foundation

- 负责人：Codex
- 目标：补真实本地运行基础设施，并把"应用创建/列表"主链路定型
- 已完成：
  - 父工程增加 SQLite JDBC 和 Flyway 版本管理
  - `pcm-kms-server` 增加 JDBC、Flyway、SQLite 依赖
  - 补充 SQLite / MySQL 数据源配置位
  - 新增 `sql/sqlite/V1__init.sql`
  - 新增 `sql/mysql/V1__init.sql`
  - 新增最小领域对象 `ClientApp`
  - 新增最小接口：
    - `POST /api/admin/apps`
    - `GET /api/admin/apps`
  - 当前 `ClientApp` 链路先使用内存存储，目的是先把接口与服务边界固定下来
- 当前状态：
  - 本地模式所需的依赖和目录已经补齐
  - 应用管理最小链路已经从"空骨架"进入"可继续替换为真实持久层"
- 暂未完成：
  - Flyway 真正自动执行
  - `ClientApp` 从内存存储切换到数据库
  - 应用启用动作和接入材料生成
  - 前端与应用接口联调

## 2026-06-10 Phase 2 核心 KMS 能力闭环

- 负责人：bot5
- 目标：从内存存储切换到真实 DB 持久化，实现核心密钥管理和加解密闭环
- 已完成：

### 后端基础设施

  - 父 POM 升级到 `0.2.0-SNAPSHOT`，新增依赖管理：
    - Sa-Token 1.37
    - Hutool 5.8.30 (BOM)
    - BouncyCastle 1.79
    - Knife4j 4.3.0
    - Caffeine 2.9.3
    - MySQL Connector 8.0.33
    - Lombok
  - `pcm-kms-infra` 集成 MyBatis-Plus + JDBC + Flyway + MySQL/SQLite 驱动
  - `pcm-kms-infra` 配置本地 Caffeine 缓存降级
  - `pcm-kms-server` 集成 Sa-Token + Knife4j + AOP
  - Maven 编译通过（全部 6 个模块 BUILD SUCCESS）

### 领域模型

  - `ClientApp` - 客户端应用（扩展了 clientId/clientSecret/signPublicKey/enabled 等字段）
  - `KeyMetadata` - 密钥元数据（algorithm/cryptoType/alias/keyVersion 等）
  - `KeyMaterial` - 密钥材料（publicKey/privateKey/secretKey）
  - `ClientKeyPermission` - 客户端密钥授权关系
  - `AuditLog` - 审计日志
  - `CryptoResult` - 加解密结果 DTO

### 公共模块

  - `AlgorithmEnum` - 加密算法枚举（RSA/SM2/AES/SM4/SIGN/MD5/SM3）
  - `CryptoTypeEnum` - 加密类型枚举
  - `KeyPurposeEnum` - 密钥用途枚举
  - `KeySourceEnum` - 密钥来源枚举
  - `OperationEnum` - 操作类型枚举（审计用）
  - `KmsException` - 业务异常
  - `IdUtil` - ID 生成工具

### 加密核心（pcm-kms-core）

  - `AlgorithmStrategy` - 算法策略接口
  - `AesAlgorithmStrategy` - AES-256-CBC 对称加密
  - `Sm4AlgorithmStrategy` - SM4 国密对称加密
  - `RsaAlgorithmStrategy` - RSA-2048 非对称加密
  - `Sm2AlgorithmStrategy` - SM2 国密非对称加密
  - `SignAlgorithmStrategy` - SHA256withRSA 签名验签
  - `Md5AlgorithmStrategy` - MD5 摘要
  - `Sm3AlgorithmStrategy` - SM3 国密摘要
  - `CryptoService` - 加密服务门面（策略模式调度）

### 业务服务（pcm-kms-server）

  - `ClientAppService` - 应用管理（创建、启用生成凭证和默认密钥、列表查询）
  - `KeyService` - 密钥管理（创建、列表、启用/禁用、轮转、公钥获取）
  - `CryptoBizService` - 加解密业务（加密、解密、签名、验签、摘要）
  - `AuditLogService` - 审计日志

### API 接口

  - `POST /api/admin/apps` - 创建应用
  - `GET /api/admin/apps` - 应用列表
  - `GET /api/admin/apps/{id}` - 应用详情
  - `POST /api/admin/apps/{id}/enable` - 启用应用
  - `POST /api/admin/keys` - 创建密钥
  - `GET /api/admin/keys` - 密钥列表
  - `GET /api/admin/keys/{id}` - 密钥详情
  - `POST /api/admin/keys/{id}/enable` - 启用密钥
  - `POST /api/admin/keys/{id}/disable` - 禁用密钥
  - `POST /api/admin/keys/{id}/rotate` - 密钥轮转
  - `POST /api/crypto/encrypt` - 加密
  - `POST /api/crypto/decrypt` - 解密
  - `POST /api/crypto/sign` - 签名
  - `POST /api/crypto/verify` - 验签
  - `POST /api/crypto/digest` - 摘要
  - `GET /api/crypto/public-key/{alias}` - 获取公钥
  - `GET /api/system/ping` - 探活

### 配置

  - 全局异常处理（GlobalExceptionHandler）
  - CORS 跨域配置
  - MyBatis-Plus 分页插件 + 驼峰映射 + 逻辑删除
  - Knife4j OpenAPI 文档
  - Flyway 配置（SQLite 模式关闭，MySQL 模式开启）

### 数据库脚本

  - `sql/sqlite/V1__init.sql` - SQLite 全量表（5 张业务表 + 1 张用户表）
  - `sql/mysql/V1__init.sql` - MySQL 全量表

- 当前状态：
  - 后端全部 6 个模块编译通过
  - 核心密钥管理和加解密 API 已实现
  - 应用启用后自动生成 clientId/clientSecret/签名密钥/默认 AES 密钥

- 暂未完成：
  - 实际运行验证（SQLite 模式启动测试）
  - 前端管理后台（Vue 3 + Element Plus 实际页面）
  - 客户端签名验证 Filter
  - 客户端密钥授权校验（当前 API 未校验 clientId 是否有权限访问 alias）
  - Sa-Token 登录认证流程
  - 限流
  - 前端联调

- 下一步建议：
  - 验证 SQLite 模式实际启动
  - 开始前端管理后台开发（Phase 3）

## 2026-06-10 Phase 3 接入体验可用

- 负责人：bot5
- 目标：前端管理后台可用 + 客户端 SDK 可用
- 已完成：

### 后端修复

  - 修复 Knife4j 兼容性：从 openapi2(springfox) 切换到 openapi3(springdoc)
  - 修复 Spring Boot repackage：添加 repackage goal
  - 修复 Controller BOM 字符问题
  - 后端 SQLite 模式实际启动验证通过（8080 端口）
  - `/api/system/ping` 接口实测可用

### 前端管理后台（pcm-kms-admin-ui）

  - 技术栈：Vue 3 + Vite + Element Plus + Vue Router + Pinia + Axios
  - 侧边栏布局（App.vue）：应用管理、密钥管理、加解密测试、首页
  - Dashboard 首页：统计卡片（应用数、密钥数、系统状态）
  - 应用管理页（AppList.vue）：
    - 应用列表（表格展示）
    - 创建应用（弹窗表单）
    - 启用应用（生成 clientId/Secret + 默认密钥）
    - 查看详情（显示 clientId/Secret 等接入信息）
  - 密钥管理页（KeyList.vue）：
    - 密钥列表（表格展示，算法/类型/版本/状态）
    - 创建密钥（弹窗表单，支持 AES/SM4/RSA/SM2/SIGN）
    - 启用/禁用密钥
    - 密钥轮转
  - 加解密测试页（CryptoTest.vue）：
    - 加密面板（输入别名+明文 → 输出密文）
    - 解密面板（输入别名+密文 → 输出明文）
    - 摘要面板（支持 MD5/SM3）
  - API 封装（api/http.js + api/index.js）：统一请求拦截 + 错误提示
  - Vite 代理配置：`/api` → `http://localhost:8080`

### 客户端 SDK（pcm-kms-client-starter）

  - `KmsClient` - 编程式加解密 API：
    - `encrypt(plainText, alias)` - 加密
    - `decrypt(cipherText, alias)` - 解密
    - `sign(data, alias)` - 签名
    - `verify(data, signature, alias)` - 验签
    - `digest(plainText, algorithm)` - 摘要
    - `getPublicKey(alias)` - 获取公钥
  - 自动签名：HMAC-SHA256（clientId + timestamp + nonce + body）
  - 配置属性：`kms.client.server-url` / `kms.client.client-id` / `kms.client.client-secret` / `kms.client.client-group`
  - Spring Boot 自动配置（META-INF/spring.factories）
  - 注解定义：
    - `@KmsDecrypt` - 方法参数级自动解密
    - `@KmsCryptoField` - 字段级加解密标注

- 当前状态：
  - 后端启动验证通过
  - 前端代码完成，npm install 通过
  - SDK 编码完成，编译通过

- 暂未完成：
  - 前端实际运行验证（npm run dev）
  - 前端与后端 API 联调测试
  - `@KmsCryptoField` AOP 切面实现（字段级自动加解密）
  - `@KmsDecrypt` 参数解析器实现
  - Sa-Token 登录认证流程
  - 客户端签名验证 Filter

- 下一步建议：
  - 验证前端 npm run dev 正常启动
  - 联调测试完整闭环
  - 补 AOP 切面实现字段级加解密
